# hash-dot-notation
instead of use
```ruby
hash = {foo: 'foo'}
hash[:foo] # => 'foo'
#or 
hash = {'foo' => 'foo'}
hash['foo'] # => 'foo'
#
```
Use
```ruby
hash = {foo: 'foo'}
hash.foo # => 'foo'

```
